version: '3.8'

services:
  web-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tgbot_app
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./database.sqlite3:/app/database.sqlite3
